APP:
------------------------------------------
Run App: npm run dev

PRISMA:
------------------------------------------
Open Prisma Studio: npx prisma studio

RESET AND TEST:
------------------------------------------
# Reset : Drop & recreate using migrations + seed (recommended)
npx prisma migrate reset --force
npx prisma generate
npm run dev

# Open Prisma strudio
npx prisma studio

# Expect: []
Invoke-WebRequest http://localhost:3000/api/elements
Invoke-WebRequest http://localhost:3000/api/recipes

# Create a new combo (mock provider):
Invoke-WebRequest http://localhost:3000/api/combine `
  -Method POST `
  -Body (@{left="water"; right="fire"} | ConvertTo-Json) `
  -ContentType "application/json"

# New Recipe
Invoke-WebRequest http://localhost:3000/api/combine `
  -Method POST `
  -Body (@{left="energy"; right="rain"} | ConvertTo-Json) `
  -ContentType "application/json"

GIT PUSHES
------------------------------------------
git add .
git commit -m "ADD DESCRIPTION"
git push --set-upstream origin <branch>

GIT UPDATES
------------------------------------------
git checkout main
git pull origin main
git checkout -b feat/<your-change>


(Invoke-WebRequest http://localhost:3000/api/combine -Method POST `
  -Body (@{ left='water'; right='water' } | ConvertTo-Json) `
  -ContentType 'application/json').Content | ConvertFrom-Json

(Invoke-WebRequest http://localhost:3000/api/combine -Method POST `
  -Body (@{ left='water'; right='human' } | ConvertTo-Json) `
  -ContentType 'application/json').Content | ConvertFrom-Json

(Invoke-WebRequest http://localhost:3000/api/combine -Method POST `
  -Body (@{ left='thunderstorm'; right='wind' } | ConvertTo-Json) `
  -ContentType 'application/json').Content | ConvertFrom-Json


Queries:

I want to update the user interface on the PlayShell.tsx page so that it has more of a modern feel and can navigate the app with ease. To do this I want to make the canvas cover the full screen and have the other components like the catalog to be floating on the canvas with rounded edges - similar to how FigJam does it on their webpage. I also want to have a profile emoji button on the top which can be clicked to open up the small drop down menu for the login/logout and profile page (not yet built). You can also put in a placeholder logo for on the top left of the page.