# Copy to .env.local and fill in the secrets

# DATABASE_URL="file:./dev.db"              # dev sqlite; prod will be Postgres
AUTH_SECRET=changeme
OPENAI_API_KEY=changeme

# Optional SMTP (leave unset in dev to log emails)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
EMAIL_FROM="OpenCraft <no-reply@yourdomain.com>"

MODEL_PROVIDER=openai
OPENAI_MODEL=gpt-4o-mini
APP_URL=http://localhost:3000
DEBUG_COMBINE=1
